import Phaser from 'phaser';

export class MercatorumLab extends Phaser.Scene {
  constructor() {
    super({ key: 'MercatorumLab' });
  }

  preload() {
    // Qui carichiamo assets come tilemap, tileset e spritesheet
    this.load.image('tiles', 'assets/tileset/mercatorum_tileset.png');
    // Placeholder per il caricamento di tilemap, sostituire con file effettivo
    // this.load.tilemapTiledJSON('map', 'assets/tileset/mercatorum_lab.json');
    // Placeholder per lo spritesheet dei ricercatori
    // this.load.spritesheet('researchers', 'assets/sprites/researchers.png', { frameWidth: 32, frameHeight: 32 });
  }

  create() {
    // Placeholder per la creazione della tilemap, decommentare quando sono disponibili gli asset
    /*
    const map = this.make.tilemap({ key: 'map' });
    const tileset = map.addTilesetImage('mercatorum_tileset', 'tiles');
    
    // Crea i layer
    const floorLayer = map.createLayer('Floor', tileset, 0, 0);
    const wallsLayer = map.createLayer('Walls', tileset, 0, 0);
    const furnitureLayer = map.createLayer('Furniture', tileset, 0, 0);
    
    // Imposta le collisioni
    wallsLayer.setCollisionByProperty({ collides: true });
    furnitureLayer.setCollisionByProperty({ collides: true });
    */
    
    // Per ora, disegniamo un quadrato colorato come placeholder
    this.add.rectangle(400, 300, 800, 600, 0x6b4f2d).setOrigin(0.5);
    this.add.text(400, 300, 'Università Mercatorum Lab', {
      color: '#ffffff',
      fontSize: '24px'
    }).setOrigin(0.5);
    
    // Aggiungi informazioni di debug
    this.add.text(10, 10, 'Mercatorum Lab - Placeholder', {
      fontSize: '16px',
      color: '#ffffff'
    });
  }

  update(time: number, delta: number) {
    // Logica di aggiornamento
  }
}