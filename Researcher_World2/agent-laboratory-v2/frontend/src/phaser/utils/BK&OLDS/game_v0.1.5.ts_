import Phaser from 'phaser';
import { MercatorumLab } from '../../scenes/MercatorumLab';
import { BlekingeLabScene } from '../../scenes/BlekingeLabScene';
import { OPBGLabScene } from '../../scenes/OPBGLabScene';

export const gameConfig: Phaser.Types.Core.GameConfig = {
  type: Phaser.AUTO,
  width: 800,
  height: 600,
  parent: 'game-container', // Questo è il valore predefinito, può essere sovrascritto
  physics: {
    default: 'arcade',
    arcade: {
      gravity: { x:0, y: 0 },
      debug: false
    }
  },
  scene: [MercatorumLab, BlekingeLabScene, OPBGLabScene],
  scale: {
    mode: Phaser.Scale.FIT,
    autoCenter: Phaser.Scale.CENTER_BOTH
  },
  render: {
    pixelArt: true,
    antialias: false,
    roundPixels: true,
    batchSize: 512  // Ridotto da 2048 per evitare problemi di memoria
  },
  // Controllo del caricamento delle risorse
  loader: {
    maxParallelDownloads: 4
  },
  // Ottimizzazioni della memoria
  banner: false,
  backgroundColor: '#333333',
  // Disabilita sistemi non necessari per ridurre il carico di memoria
  audio: {
    disableWebAudio: true
  },
  // Limitazione dell'utilizzo del DOM per evitare perdite di memoria
  dom: {
    createContainer: false
  }
};

// Classe principale del gioco
export class Game extends Phaser.Game {
  constructor(config: Phaser.Types.Core.GameConfig) {
    super(config);
  }
}

// Inizializzazione del gioco
// Modificata per accettare un containerId opzionale
export function initGame(containerId?: string): Phaser.Game {
  // Esegui la pulizia preventiva della memoria
  if (window.gc) {
    window.gc();
  }
  
  const config = { ...gameConfig };
  if (containerId) {
    config.parent = containerId;
  }
  return new Game(config);
}